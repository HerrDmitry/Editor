# Editor

use dotnet build to build 

if you are trying to run it on Linux and **NVIDIA** is you GPU, there is known issue with **Photino**. Use **run.sh** or set **__NV_DISABLE_EXPLICIT_SYNC=1** globally
