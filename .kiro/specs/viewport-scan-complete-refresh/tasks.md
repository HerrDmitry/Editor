# Implementation Plan

- [x] 1. Write bug condition exploration test
  - **Property 1: Bug Condition** - No ViewportResponse After Large File Scan Completion
  - **CRITICAL**: This test MUST FAIL on unfixed code - failure confirms the bug exists
  - **DO NOT attempt to fix the test or the code when it fails**
  - **NOTE**: This test encodes the expected behavior - it will validate the fix when it passes after implementation
  - **GOAL**: Surface counterexamples that demonstrate the bug exists
  - **Scoped PBT Approach**: Scope property to large files (>256 KB) where partial metadata is emitted and scan completes successfully
  - Create test in `tests/EditorApp.Tests/` using xUnit + FsCheck 3.1.0
  - Mock `IFileService` to simulate large file scan: `OpenFileAsync` invokes `onPartialMetadata` callback (indicating >256 KB file), then returns final `FileOpenMetadata`
  - Mock `IMessageRouter` to capture all sent messages in order
  - Mock `IViewportService` to return a valid `ViewportResult`
  - Instantiate `PhotinoHostService` via internal test constructor with mocked dependencies
  - Call `OpenFileByPathAsync` with a file path
  - Assert: after the final `FileOpenedResponse` (isPartial=false), a `ViewportResponse` is sent
  - Bug condition from design: `isBugCondition(input)` = fileSize > 256_000 AND partialMetadataWasEmitted AND NOT viewportResponseSentAfterScanComplete
  - Expected behavior: `ViewportResponse` with `totalPhysicalLines == finalTotalLines` and `startLine >= 0` is sent after final `FileOpenedResponse`
  - Use FsCheck to generate random file sizes > 256_000, random total line counts, random viewport params
  - Run test on UNFIXED code
  - **EXPECTED OUTCOME**: Test FAILS (proves bug exists — no ViewportResponse sent after scan complete)
  - Document counterexamples found (e.g., "For fileSize=300000, totalLines=500: messages contain FileOpenedResponse(isPartial:false) but no subsequent ViewportResponse")
  - Mark task complete when test is written, run, and failure is documented
  - _Requirements: 1.1, 1.2, 2.1, 2.2_

- [x] 2. Write preservation property tests (BEFORE implementing fix)
  - **Property 2: Preservation** - Small File Opens and Non-Scan-Complete Flows Unchanged
  - **IMPORTANT**: Follow observation-first methodology
  - Create test in `tests/EditorApp.Tests/` using xUnit + FsCheck 3.1.0
  - **Observation phase** (run on UNFIXED code):
    - Observe: Opening a small file (≤256 KB, no `onPartialMetadata` invoked) produces exactly one `FileOpenedResponse` and zero pushed `ViewportResponse` messages
    - Observe: `HandleRequestViewportAsync` responds with `ViewportResponse` only when explicitly requested (on-demand)
    - Observe: Scan cancellation (CancellationToken cancelled during scan) produces no `FileOpenedResponse` and no `ViewportResponse` for cancelled file
  - **Property-based tests**:
    - Property 2a: For all file sizes ≤ 256_000 (generated by FsCheck), `OpenFileByPathAsync` sends exactly one `FileOpenedResponse(isPartial:false)` and zero `ViewportResponse` messages
    - Property 2b: For all valid `RequestViewport` messages (generated by FsCheck with random startLine, lineCount, startColumn, columnCount, wrapMode, viewportColumns), `HandleRequestViewportAsync` sends exactly one `ViewportResponse` — no extra messages
    - Property 2c: When scan is cancelled mid-flight, no `ViewportResponse` is sent for the cancelled file
  - Mock `IFileService`: for small files, `OpenFileAsync` does NOT invoke `onPartialMetadata` and returns metadata directly
  - Mock `IMessageRouter` to capture all sent messages
  - Mock `IViewportService` to return valid `ViewportResult`
  - Verify tests pass on UNFIXED code
  - **EXPECTED OUTCOME**: Tests PASS (confirms baseline behavior to preserve)
  - Mark task complete when tests are written, run, and passing on unfixed code
  - _Requirements: 3.1, 3.2, 3.3, 3.4_

- [x] 3. Fix for viewport-scan-complete-refresh

  - [x] 3.1 Add `_lastViewportRequest` field to `PhotinoHostService`
    - Add field: `private volatile RequestViewport? _lastViewportRequest;`
    - In `HandleRequestViewportAsync`, store `request` into `_lastViewportRequest` before processing
    - _Bug_Condition: isBugCondition(input) where fileSize > 256_000 AND partialMetadataWasEmitted AND NOT viewportResponseSentAfterScanComplete_
    - _Expected_Behavior: System stores last viewport request params for replay after scan completion_
    - _Preservation: HandleRequestViewportAsync continues to respond identically to on-demand requests_
    - _Requirements: 2.1, 2.2, 3.2_

  - [x] 3.2 Add viewport push after final `FileOpenedResponse` in `OpenFileByPathAsync` (backend safety net)
    - Track `partialWasEmitted` flag: set to `true` inside `onPartialMetadata` callback
    - After sending final `FileOpenedResponse` (when `partialWasEmitted == true` AND `_viewportService != null`):
      - Read `_lastViewportRequest` (may be null if no viewport requested during scan)
      - Use stored params or defaults: startLine=0, lineCount=100, startColumn=0, columnCount=200, wrapMode=false, viewportColumns=200
      - Call `_viewportService.GetViewportAsync(filePath, startLine, lineCount, startColumn, columnCount, wrapMode, viewportColumns, scanToken)`
      - Send resulting `ViewportResponse` to UI via `_messageRouter.SendToUIAsync`
    - Guard: check `scanToken` cancellation before viewport push
    - Guard: wrap viewport push in try/catch to not break scan-complete flow on viewport errors
    - _Expected_Behavior: ViewportResponse sent after FileOpenedResponse(isPartial:false) for large files (safety net for viewport-protocol consumers)_
    - _Preservation: Small files (no partialWasEmitted) skip viewport push entirely_
    - _Requirements: 2.1, 3.1, 3.4_

  - [x] 3.3 Frontend: re-request lines on scan complete in `App.tsx` (primary fix)
    - In `onFileOpened` callback, the branch handling final (non-partial, non-refresh) `FileOpenedResponse` for the same file:
      - After updating `fileMeta` and clearing `loadProgress`, re-request current buffer range via `sendRequestLines`
      - Use same pattern as `isRefresh` handler: clamp start if file shrunk, request `bufferLen` lines, set `isJumpRequestRef = true`
    - _Bug_Condition: Frontend receives scan-complete metadata but doesn't refresh displayed lines_
    - _Expected_Behavior: Frontend re-requests lines → ContentArea displays fully-indexed content_
    - _Preservation: Small file opens (different fileName) still do full reset; refresh cycle unchanged_
    - _Requirements: 1.1, 1.2, 2.2, 2.3_

  - [x] 3.4 Verify bug condition exploration test now passes
    - **Property 1: Expected Behavior** - ViewportResponse After Large File Scan Completion
    - **IMPORTANT**: Re-run the SAME test from task 1 - do NOT write a new test
    - The test from task 1 encodes the expected behavior
    - When this test passes, it confirms the expected behavior is satisfied
    - Run bug condition exploration test from step 1
    - **EXPECTED OUTCOME**: Test PASSES (confirms bug is fixed)
    - _Requirements: 2.1, 2.2_

  - [x] 3.5 Verify preservation tests still pass
    - **Property 2: Preservation** - Small File Opens and Non-Scan-Complete Flows Unchanged
    - **IMPORTANT**: Re-run the SAME tests from task 2 - do NOT write new tests
    - Run preservation property tests from step 2
    - **EXPECTED OUTCOME**: Tests PASS (confirms no regressions)
    - Confirm all tests still pass after fix (no regressions)

- [x] 4. Checkpoint - Ensure all tests pass
  - Run full test suite: `dotnet build tests/EditorApp.Tests` then `timeout 120 dotnet test tests/EditorApp.Tests --no-build`
  - Ensure all tests pass, ask the user if questions arise.
